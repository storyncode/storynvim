---
phase: 01-bootstrap-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - lua/story/bootstrap.lua
  - lua/story/bootstrap/lazy.lua
autonomous: true
requirements:
  - BOOT-02
  - BOOT-03
must_haves:
  truths:
    - "The modular startup path still bootstraps `lazy.nvim` correctly after the clone and runtimepath logic is extracted."
    - "The final Phase 1 startup flow preserves the required ordering of early globals, bootstrap helper execution, and `lazy.nvim` setup."
    - "`init.lua` remains thin while the bootstrap internals stay organized enough for later phases without changing current behavior."
  artifacts:
    - path: "lua/story/bootstrap/lazy.lua"
      provides: "Shared helper for cloning and prepending lazy.nvim"
    - path: "lua/story/bootstrap.lua"
      provides: "Bootstrap module calling the helper before `require('lazy').setup`"
  key_links:
    - from: "lua/story/bootstrap.lua"
      to: "lua/story/bootstrap/lazy.lua"
      via: "require('story.bootstrap.lazy').setup()"
      pattern: "require\\('story\\.bootstrap\\.lazy'\\)\\.setup\\(\\)"
    - from: "lua/story/bootstrap/lazy.lua"
      to: "vim.opt.rtp"
      via: "runtimepath prepend"
      pattern: "rtp:prepend\\(lazypath\\)"
---

<objective>
Extract the risky `lazy.nvim` bootstrap into a dedicated helper and lock down the Phase 1 startup path with reusable validation.

Purpose: isolate the remote clone/runtimepath behavior that carries the highest bootstrap risk while keeping the main bootstrap module readable and ordered.
Output: `lua/story/bootstrap/lazy.lua` plus an updated `lua/story/bootstrap.lua` that calls it before `require('lazy').setup(...)`.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
@.planning/phases/01-bootstrap-skeleton/01-VALIDATION.md
@lua/story/bootstrap.lua
@init.lua

<interfaces>
Target helper contract:
```lua
local M = {}

function M.setup()
  local lazypath = vim.fn.stdpath 'data' .. '/lazy/lazy.nvim'
  -- clone if missing, error on shell failure, prepend runtimepath
end

return M
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the lazy.nvim clone and runtimepath logic into a shared helper</name>
  <read_first>
    - lua/story/bootstrap.lua
    - init.lua
    - .planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <files>lua/story/bootstrap.lua, lua/story/bootstrap/lazy.lua</files>
  <acceptance_criteria>
    - `lua/story/bootstrap/lazy.lua` exists and exports `function M.setup()`.
    - `lua/story/bootstrap/lazy.lua` contains `https://github.com/folke/lazy.nvim.git`.
    - `lua/story/bootstrap/lazy.lua` contains `--filter=blob:none`.
    - `lua/story/bootstrap/lazy.lua` contains `--branch=stable`.
    - `lua/story/bootstrap/lazy.lua` contains `rtp:prepend(lazypath)`.
    - `lua/story/bootstrap.lua` contains `require('story.bootstrap.lazy').setup()`.
    - `lua/story/bootstrap.lua` no longer contains the inline `local lazypath = vim.fn.stdpath 'data' .. '/lazy/lazy.nvim'` block.
  </acceptance_criteria>
  <action>Create `lua/story/bootstrap/lazy.lua` with a single `M.setup()` function that owns the existing clone-if-missing logic, shell-error handling, and runtimepath prepend exactly as currently implemented. Update `lua/story/bootstrap.lua` to call `require('story.bootstrap.lazy').setup()` immediately before `require('lazy').setup(...)`. Do not add retries, alternate remotes, caching, or fallback download logic; the helper must remain a structural extraction of the current trusted behavior, not a semantic rewrite.</action>
  <verify>
    <automated>sh -c "rg -n \"function M\\.setup\\(|https://github.com/folke/lazy\\.nvim\\.git|--filter=blob:none|--branch=stable|rtp:prepend\\(lazypath\\)\" lua/story/bootstrap/lazy.lua && rg -n \"require\\('story\\.bootstrap\\.lazy'\\)\\.setup\\(\\)\" lua/story/bootstrap.lua && ! rg -n \"stdpath 'data' .. '/lazy/lazy.nvim'\" lua/story/bootstrap.lua"</automated>
  </verify>
  <done>The highest-risk bootstrap code is isolated in one helper while the main bootstrap module stays focused on startup ordering and plugin setup.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the Phase 1 smoke suite after helper extraction</name>
  <read_first>
    - init.lua
    - lua/story/bootstrap.lua
    - lua/story/bootstrap/lazy.lua
    - .planning/phases/01-bootstrap-skeleton/01-VALIDATION.md
  </read_first>
  <files>lua/story/bootstrap.lua, lua/story/bootstrap/lazy.lua</files>
  <acceptance_criteria>
    - `nvim --headless -u ./init.lua "+lua assert(vim.g.mapleader == ' ' and vim.g.maplocalleader == ' ' and type(vim.g.have_nerd_font) == 'boolean')" +qa` exits 0.
    - `nvim --headless -u ./init.lua "+Lazy! health" +qa` exits 0.
    - `nvim --headless -u ./init.lua "+checkhealth kickstart" +qa` exits 0.
    - `rg -n "require\\('story\\.bootstrap\\.lazy'\\)\\.setup\\(\\).*" lua/story/bootstrap.lua` finds the helper call before `require('lazy').setup(` in file order.
  </acceptance_criteria>
  <action>Run the same headless smoke suite used in Plan `01-02` after the helper extraction. If any command fails, treat it as a blocker because the helper split changed the remote bootstrap path. Confirm by grep or line-order inspection that `require('story.bootstrap.lazy').setup()` still happens before `require('lazy').setup(...)`, and correct the call order if needed before closing the plan.</action>
  <verify>
    <automated>sh -c "test $(rg -n \"require\\('story\\.bootstrap\\.lazy'\\)\\.setup\\(\\)\" lua/story/bootstrap.lua | cut -d: -f1) -lt $(rg -n \"require\\('lazy'\\)\\.setup\\(\" lua/story/bootstrap.lua | cut -d: -f1) && nvim --headless -u ./init.lua \"+lua assert(vim.g.mapleader == ' ' and vim.g.maplocalleader == ' ' and type(vim.g.have_nerd_font) == 'boolean')\" \"+Lazy! health\" \"+checkhealth kickstart\" +qa"</automated>
  </verify>
  <done>The extracted helper preserves the real startup path, and the modular bootstrap sequence is still validated end-to-end after the last structural extraction in Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bootstrap module -> shared helper | Ordered startup now crosses a second local module boundary |
| shared helper -> remote git clone | Remote code can still be fetched on first bootstrap |
| shared helper -> runtimepath and lazy.nvim | Helper order errors can break plugin-manager resolution before health checks run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `lua/story/bootstrap/lazy.lua` remote bootstrap | mitigate | Keep the remote URL, stable branch, clone flags, and error handling identical to the previous inline implementation |
| T-01-08 | D | helper call ordering in `lua/story/bootstrap.lua` | mitigate | Verify the helper call appears before `require('lazy').setup(...)` and rerun the full headless smoke suite after extraction |
| T-01-09 | E | hidden side effects after module split | mitigate | Require the helper explicitly from `lua/story/bootstrap.lua` and validate through real startup, not isolated Lua syntax checks |
</threat_model>

<verification>
The plan is complete only when the helper file exists with the exact trusted bootstrap values, `lua/story/bootstrap.lua` calls it before lazy setup, and the full Phase 1 smoke suite exits successfully.
</verification>

<success_criteria>
The most failure-prone bootstrap logic is isolated in a shared helper without changing behavior, and the final Phase 1 startup path remains validated against the repo’s headless Neovim checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-skeleton/01-bootstrap-skeleton-03-SUMMARY.md`
</output>
