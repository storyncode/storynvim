---
phase: 01-bootstrap-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - init.lua
  - lua/story/bootstrap.lua
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - BOOT-03
must_haves:
  truths:
    - "`init.lua` contains only early globals and one bootstrap entrypoint."
    - "Starting Neovim through `init.lua` still bootstraps `lazy.nvim` and reaches health checks."
    - "Leader globals and `have_nerd_font` are defined before the bootstrap module runs."
  artifacts:
    - path: "init.lua"
      provides: "Thin startup entrypoint"
    - path: "lua/story/bootstrap.lua"
      provides: "Module invoked by the entrypoint"
  key_links:
    - from: "init.lua"
      to: "lua/story/bootstrap.lua"
      via: "require('story.bootstrap').setup()"
      pattern: "require\\('story\\.bootstrap'\\)\\.setup\\(\\)"
    - from: "lua/story/bootstrap.lua"
      to: "lazy.nvim runtimepath"
      via: "bootstrap block before setup"
      pattern: "rtp:prepend\\(lazypath\\)"
---

<objective>
Convert `init.lua` from the monolithic runtime file into the minimal Phase 1 entrypoint.

Purpose: satisfy the visible Phase 1 outcome by making `init.lua` small while preserving the exact startup order required for `lazy.nvim`, leader keys, and early globals.
Output: a thin `init.lua` that owns only early globals and delegates to `require('story.bootstrap').setup()`.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
@.planning/phases/01-bootstrap-skeleton/01-VALIDATION.md
@init.lua
@lua/story/bootstrap.lua

<interfaces>
Required entrypoint shape after this plan:
```lua
vim.g.mapleader = ' '
vim.g.maplocalleader = ' '
vim.g.have_nerd_font = false

require('story.bootstrap').setup()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce init.lua to the exact Phase 1 startup contract</name>
  <read_first>
    - init.lua
    - lua/story/bootstrap.lua
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
  </read_first>
  <files>init.lua</files>
  <acceptance_criteria>
    - `init.lua` contains `vim.g.mapleader = ' '`.
    - `init.lua` contains `vim.g.maplocalleader = ' '`.
    - `init.lua` contains `vim.g.have_nerd_font = false`.
    - `init.lua` contains `require('story.bootstrap').setup()`.
    - `init.lua` does not contain `require('lazy').setup(`.
    - `init.lua` does not contain `stdpath 'data' .. '/lazy/lazy.nvim'`.
    - `init.lua` does not contain `vim.api.nvim_create_autocmd('TextYankPost'`.
    - Manual review confirms the only executable statements remaining in `init.lua` are the three early-global assignments and `require('story.bootstrap').setup()`.
  </acceptance_criteria>
  <action>Edit `init.lua` down to the Phase 1 minimum: keep the explanatory header comment if desired, but the only executable statements must be `vim.g.mapleader = ' '`, `vim.g.maplocalleader = ' '`, `vim.g.have_nerd_font = false`, and `require('story.bootstrap').setup()`. Remove the inlined options, keymaps, autocommands, lazy bootstrap block, and `require('lazy').setup` body from `init.lua` because they now live in `lua/story/bootstrap.lua`. Do not add any extra globals, wrapper functions, or phase-ahead imports.</action>
  <verify>
    <automated>python3 - <<'PY'\nfrom pathlib import Path\nallowed = [\n    \"vim.g.mapleader = ' '\",\n    \"vim.g.maplocalleader = ' '\",\n    \"vim.g.have_nerd_font = false\",\n    \"require('story.bootstrap').setup()\",\n]\ntext = Path('init.lua').read_text().splitlines()\nfiltered = []\nin_block = False\nfor line in text:\n    stripped = line.strip()\n    if in_block:\n        if ']]' in stripped:\n            in_block = False\n        continue\n    if stripped.startswith('--[['):\n        if ']]' not in stripped:\n            in_block = True\n        continue\n    if not stripped or stripped.startswith('--'):\n        continue\n    filtered.append(stripped)\nassert filtered == allowed, filtered\nprint('init.lua executable whitelist verified')\nPY</automated>\n    <manual>Open `init.lua` and verify it is structurally thin: after the optional header comment there are no executable statements beyond `vim.g.mapleader = ' '`, `vim.g.maplocalleader = ' '`, `vim.g.have_nerd_font = false`, and `require('story.bootstrap').setup()`.</manual>
  </verify>
  <done>`init.lua` is visibly minimal and now acts only as the ordered bootstrap entrypoint required by BOOT-01.</done>
</task>

<task type="auto">
  <name>Task 2: Run Phase 1 smoke validation through the real init.lua path</name>
  <read_first>
    - init.lua
    - lua/story/bootstrap.lua
    - .planning/phases/01-bootstrap-skeleton/01-VALIDATION.md
    - lua/kickstart/health.lua
  </read_first>
  <files>init.lua, lua/story/bootstrap.lua</files>
  <acceptance_criteria>
    - `nvim --headless -u ./init.lua "+lua assert(vim.g.mapleader == ' ' and vim.g.maplocalleader == ' ' and type(vim.g.have_nerd_font) == 'boolean')" +qa` exits 0.
    - `nvim --headless -u ./init.lua "+Lazy! health" +qa` exits 0.
    - `nvim --headless -u ./init.lua "+checkhealth kickstart" +qa` exits 0.
  </acceptance_criteria>
  <action>Use the exact smoke commands from `01-VALIDATION.md` against the real `./init.lua` entrypoint after the refactor. If any check fails, fix ordering or wiring in `init.lua` or `lua/story/bootstrap.lua` before marking the plan complete. Treat any startup failure, missing `lazy.nvim` health check, or failed early-global assertion as a blocking regression for BOOT-02 and BOOT-03.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+lua assert(vim.g.mapleader == ' ' and vim.g.maplocalleader == ' ' and type(vim.g.have_nerd_font) == 'boolean')" "+Lazy! health" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>The modular entrypoint works through the real startup path, proving that the refactor did not break `lazy.nvim` bootstrap or early-global ordering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init.lua -> bootstrap module | Minimal entrypoint hands control to new module code |
| bootstrap module -> lazy.nvim | Runtimepath bootstrap and plugin-manager setup must remain correct |
| startup path -> health checks | Lazy-loaded side effects can hide regressions until runtime validation executes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | D | `init.lua` entrypoint rewrite | mitigate | Keep only the three early globals plus `require('story.bootstrap').setup()` and verify the old lazy/bootstrap strings are absent from `init.lua` |
| T-01-05 | T | startup order across `init.lua` and `lua/story/bootstrap.lua` | mitigate | Use the early-global smoke assertion plus `Lazy! health` and `checkhealth kickstart` as blocking checks before completion |
| T-01-06 | E | lazy-loaded plugin side effects | mitigate | Validate through the full headless startup path rather than grep alone so side effects and ordering regressions surface immediately |
</threat_model>

<verification>
The plan is complete only when the grep checks against `init.lua` pass and all three headless Neovim commands from the validation contract exit successfully.
</verification>

<success_criteria>
`init.lua` is reduced to a thin bootstrap file, the modular startup path boots successfully through `lazy.nvim`, and the entrypoint still defines all required globals before the module executes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-skeleton/01-bootstrap-skeleton-02-SUMMARY.md`
</output>
