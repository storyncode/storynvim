---
phase: 01-bootstrap-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - init.lua
  - lua/story/bootstrap.lua
autonomous: true
requirements:
  - BOOT-01
  - BOOT-03
must_haves:
  truths:
    - "`init.lua` can be reduced to early globals plus one bootstrap entrypoint because the ordered startup path now lives behind a dedicated module."
    - "The modular startup path preserves the existing order for early globals, core startup behavior, lazy bootstrap, and `lazy.nvim` setup."
  artifacts:
    - path: "lua/story/bootstrap.lua"
      provides: "Ordered startup entrypoint with a public setup function"
    - path: "init.lua"
      provides: "Only early globals plus bootstrap delegation after later plans complete"
  key_links:
    - from: "init.lua"
      to: "lua/story/bootstrap.lua"
      via: "require('story.bootstrap').setup()"
      pattern: "require\\('story\\.bootstrap'\\)\\.setup"
    - from: "lua/story/bootstrap.lua"
      to: "require('lazy').setup"
      via: "ordered bootstrap body"
      pattern: "require\\('lazy'\\)\\.setup"
---

<objective>
Create the first modular startup boundary for Phase 1 without changing behavior.

Purpose: establish one auditable bootstrap module so the later `init.lua` reduction is a wiring change instead of a large in-place rewrite.
Output: a new `lua/story/bootstrap.lua` module with a public `setup()` entrypoint and the current startup body moved behind that contract.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
@.planning/phases/01-bootstrap-skeleton/01-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@init.lua

<interfaces>
From init.lua:
```lua
vim.g.mapleader = ' '
vim.g.maplocalleader = ' '
vim.g.have_nerd_font = false

local lazypath = vim.fn.stdpath 'data' .. '/lazy/lazy.nvim'
require('lazy').setup({
  -- existing plugin spec table stays functionally identical in Phase 1
}, {
  ui = {
    icons = vim.g.have_nerd_font and {} or { ... },
  },
})
```

Target contract to create:
```lua
local M = {}

function M.setup()
  -- ordered startup body moved from init.lua
end

return M
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the bootstrap module contract and move the ordered startup body behind it</name>
  <read_first>
    - init.lua
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <files>lua/story/bootstrap.lua</files>
  <acceptance_criteria>
    - `lua/story/bootstrap.lua` exists and contains `local M = {}`.
    - `lua/story/bootstrap.lua` exports `function M.setup()`.
    - `lua/story/bootstrap.lua` contains `require('lazy').setup(`.
    - `lua/story/bootstrap.lua` still contains the existing `TextYankPost` autocmd definition.
    - `lua/story/bootstrap.lua` does not assign `vim.g.mapleader`, `vim.g.maplocalleader`, or `vim.g.have_nerd_font`.
  </acceptance_criteria>
  <action>Create `lua/story/bootstrap.lua` and move the current startup body from `init.lua` into `M.setup()` while preserving the existing order from the current file: options first, diagnostic config and basic keymaps second, `TextYankPost` autocmd third, `lazy.nvim` bootstrap block fourth, and `require('lazy').setup({...}, {...})` last. Keep the plugin spec table and lazy UI options byte-for-byte equivalent except for indentation or local helper wrappers. Do not create `lua/plugins` files, do not split options/keymaps/autocmds into separate modules, and do not change plugin declarations in this plan.</action>
  <verify>
    <automated>sh -c "rg -n \"local M = \\{\\}|function M\\.setup\\(|TextYankPost|require\\('lazy'\\)\\.setup\" lua/story/bootstrap.lua && ! rg -n \"vim\\.g\\.mapleader|vim\\.g\\.maplocalleader|vim\\.g\\.have_nerd_font\" lua/story/bootstrap.lua"</automated>
  </verify>
  <done>`lua/story/bootstrap.lua` exists as the single ordered bootstrap module, and all startup code after the early globals has been staged behind `M.setup()` without Phase 2 or Phase 3 restructuring.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the exact early-global boundary in the module design per BOOT-03</name>
  <read_first>
    - init.lua
    - lua/story/bootstrap.lua
    - .planning/phases/01-bootstrap-skeleton/01-RESEARCH.md
    - .planning/phases/01-bootstrap-skeleton/01-VALIDATION.md
  </read_first>
  <files>lua/story/bootstrap.lua</files>
  <acceptance_criteria>
    - `lua/story/bootstrap.lua` reads `vim.g.have_nerd_font` inside plugin setup or plugin config.
    - `lua/story/bootstrap.lua` does not set any leader globals.
    - The first `require('lazy').setup(` in `lua/story/bootstrap.lua` appears after the lazy bootstrap block.
    - The module exposes a single public entrypoint `M.setup()` and no implicit execution at file load time.
  </acceptance_criteria>
  <action>Audit the new module so it consumes early globals that remain owned by `init.lua` per BOOT-03. Keep `vim.g.have_nerd_font` reads intact where they already influence plugin specs or UI config, but prevent any startup side effects from happening at module `require` time by ensuring all executable statements live inside `M.setup()`. Preserve the `lazy.nvim` clone URL `https://github.com/folke/lazy.nvim.git`, `--filter=blob:none`, `--branch=stable`, and `vim.opt.rtp:prepend(lazypath)` sequence exactly as the existing trusted path.</action>
  <verify>
    <automated>python3 - <<'PY'\nfrom pathlib import Path\ntext = Path('lua/story/bootstrap.lua').read_text()\nassert 'vim.g.mapleader' not in text\nassert 'vim.g.maplocalleader' not in text\nassert 'vim.g.have_nerd_font =' not in text\nsetup_idx = text.index('function M.setup()')\nlazy_idx = text.index(\"require('lazy').setup\")\nrtp_idx = text.index('rtp:prepend(lazypath)')\nassert setup_idx < rtp_idx < lazy_idx\nprefix = text[:setup_idx]\nfor forbidden in ['vim.o.', 'vim.opt.', 'vim.keymap.set', 'vim.api.nvim_create_autocmd', \"require('lazy').setup\", 'rtp:prepend(lazypath)']:\n    assert forbidden not in prefix, forbidden\nprint('bootstrap module ordering and require-time boundary verified')\nPY</automated>
  </verify>
  <done>The bootstrap module is safe to require later from a thin `init.lua` because order-sensitive work stays inside `M.setup()` and still consumes the early globals from the entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local config -> remote git clone | First bootstrap may execute `git clone` against GitHub when `lazy.nvim` is missing |
| early globals -> bootstrap module | Order-sensitive globals cross from `init.lua` into module-owned startup code |
| bootstrap module -> lazy.nvim/plugin configs | Side effects inside `require('lazy').setup(...)` can hide regressions until runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `lua/story/bootstrap.lua` lazy clone block | mitigate | Keep the clone URL, stable branch, shell-error check, and `rtp:prepend` sequence identical to the current trusted implementation; do not introduce new remote sources in this plan |
| T-01-02 | E | `lua/story/bootstrap.lua` module load path | mitigate | Put all side-effecting startup code inside `M.setup()` so `require('story.bootstrap')` alone cannot change execution order |
| T-01-03 | D | `lua/story/bootstrap.lua` startup ordering | mitigate | Preserve the current section order and verify `require('lazy').setup` appears only after runtimepath bootstrap |
</threat_model>

<verification>
Run the task-level grep checks, then inspect `lua/story/bootstrap.lua` to confirm it is a single ordered startup boundary and not a Phase 2/3 reorganization.
</verification>

<success_criteria>
The repo has a reusable bootstrap module with a public `setup()` entrypoint, no early globals have been moved out of `init.lua`, and the risky `lazy.nvim` setup path is preserved behind a single explicit boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-skeleton/01-bootstrap-skeleton-01-SUMMARY.md`
</output>
