---
phase: 05-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "A reader can identify `init.lua` as a thin entrypoint instead of the primary customization surface."
    - "A reader can see where shipped plugin behavior, always-on editor behavior, and fork-local extensions now live."
    - "The README no longer describes this fork as an intentionally single-file configuration."
  artifacts:
    - path: "README.md"
      provides: "Primary onboarding, architecture map, and customization guidance for the modular layout"
      contains:
        - "require('story.bootstrap').setup()"
        - "lua/core/"
        - "lua/plugins/"
        - "lua/custom/plugins/"
  key_links:
    - from: "README.md"
      to: "init.lua"
      via: "entrypoint explanation"
      pattern: "story\\.bootstrap"
    - from: "README.md"
      to: "lua/custom/plugins/init.lua"
      via: "fork-local extension guidance"
      pattern: "lua/custom/plugins/"
---

<objective>
Rewrite the primary repo guide so it teaches the modular StoryNvim layout that now exists instead of the historical single-file Kickstart framing.

Purpose: Satisfy DOC-01 and DOC-02 by making the README the accurate source of truth for architecture and safe customization.
Output: An updated `README.md` that explains the runtime layout, active customization paths, and current project positioning.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-documentation-refresh/05-CONTEXT.md
@.planning/phases/05-documentation-refresh/05-RESEARCH.md
@.planning/phases/05-documentation-refresh/05-VALIDATION.md
@.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
@README.md
@init.lua
@lua/story/bootstrap.lua
@lua/core/options.lua
@lua/core/keymaps.lua
@lua/core/autocmds.lua
@lua/plugins/editor.lua
@lua/plugins/lsp.lua
@lua/custom/plugins/init.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe README identity and architecture around the modular runtime</name>
  <read_first>
    .planning/phases/05-documentation-refresh/05-CONTEXT.md
    .planning/phases/05-documentation-refresh/05-RESEARCH.md
    .planning/phases/05-documentation-refresh/05-VALIDATION.md
    README.md
    init.lua
    lua/story/bootstrap.lua
    lua/core/options.lua
    lua/core/keymaps.lua
    lua/core/autocmds.lua
    lua/plugins/editor.lua
    lua/plugins/lsp.lua
  </read_first>
  <files>README.md</files>
  <acceptance_criteria>
    `rg -n "story.bootstrap|lua/core/|lua/plugins/|lua/custom/plugins/" README.md` returns matches.
    `rg -n "Single-file|single-file|single file" README.md` returns no matches.
    The README introduction and architecture/customization sections describe the repo as a modular personal or forkable config per D-01, D-02, and D-05.
  </acceptance_criteria>
  <action>
    Rewrite `README.md` so the introduction and first architecture-oriented sections stop presenting the repo as `kickstart.nvim` and stop teaching `init.lua` as the main place to learn or customize the config. Add a concise architecture map or file-tree-style section per D-02 that explicitly explains `init.lua` as the thin entrypoint calling `require('story.bootstrap').setup()`, `lua/story/` as startup orchestration, `lua/core/` as always-on editor behavior, `lua/plugins/` as shipped plugin behavior, and `lua/custom/plugins/` as the stable fork-local extension path per D-03. Keep still-accurate install and dependency content per D-06, but rewrite any prose that depends on the old single-file teaching model.
  </action>
  <verify>
    <automated>sh -c "rg -n 'story.bootstrap|lua/core/|lua/plugins/|lua/custom/plugins/' README.md && ! rg -n 'Single-file|single-file|single file' README.md"</automated>
  </verify>
  <done>
    `README.md` teaches the actual modular runtime layout and contains no remaining single-file positioning for this fork.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace stale README customization guidance and FAQ language</name>
  <read_first>
    .planning/phases/05-documentation-refresh/05-CONTEXT.md
    .planning/phases/05-documentation-refresh/05-RESEARCH.md
    README.md
    lua/custom/plugins/init.lua
    .planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <acceptance_criteria>
    `rg -n "lua/custom/plugins/|lua/plugins/|lua/core/" README.md` returns matches in onboarding or customization guidance.
    `rg -n "kickstart-modular|Why is the kickstart \`init.lua\` a single file|Read through the \`init.lua\`" README.md` returns no matches.
    Legacy `lua/kickstart/plugins/` content, if mentioned, is described as compatibility or example-only per D-04.
  </acceptance_criteria>
  <action>
    Update the README sections that currently instruct users to read the monolithic `init.lua`, defend the old single-file architecture, or route customization through outdated Kickstart framing. Replace that guidance with practical customization instructions per D-03 and D-04: point users to `lua/plugins/` for shipped plugin behavior, `lua/core/` for always-on behavior, and `lua/custom/plugins/` for fork-local additions, while describing `lua/kickstart/plugins/` only as legacy or example opt-ins. Preserve useful install, dependency, and troubleshooting content where still accurate per D-06.
  </action>
  <verify>
    <automated>sh -c "rg -n 'lua/core/|lua/plugins/|lua/custom/plugins/' README.md && ! rg -n 'kickstart-modular|Why is the kickstart `init.lua` a single file|Read through the `init.lua`' README.md"</automated>
  </verify>
  <done>
    README onboarding and FAQ guidance point to current customization surfaces and no longer justify the single-file model for this fork.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader -> README guidance | Users rely on README wording to decide which files are safe to edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | README.md customization guidance | mitigate | Replace stale guidance with exact active paths per D-03/D-04 and verify with `rg` against `story.bootstrap`, `lua/core/`, `lua/plugins/`, and `lua/custom/plugins/`. |
| T-05-02 | I | README.md project identity | mitigate | Remove single-file framing and Kickstart-modular FAQ references so the repo is not misrepresented as monolithic per D-01/D-02. |
</threat_model>

<verification>
Run the README-scoped validation command after each task:

```bash
sh -c "rg -n \"story.bootstrap|lua/core/|lua/plugins/|lua/custom/plugins/\" README.md && ! rg -n \"Single-file|single-file|single file\" README.md"
```
</verification>

<success_criteria>
`README.md` is sufficient for a new reader to understand the modular layout, active customization paths, and the fact that this fork no longer treats the single-file layout as the intended architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-refresh/05-documentation-refresh-01-SUMMARY.md`
</output>
