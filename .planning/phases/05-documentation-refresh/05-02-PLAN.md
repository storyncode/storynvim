---
phase: 05-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - doc/kickstart.txt
  - doc/tags
  - init.lua
  - lua/custom/plugins/init.lua
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Inline and help-doc guidance matches the modular README story instead of teaching the monolithic config as the intended model."
    - "Users opening Neovim help still find valid tags after help-text changes."
    - "Compatibility surfaces that still use Kickstart naming remain documented as compatibility names, not as the preferred customization path."
  artifacts:
    - path: "doc/kickstart.txt"
      provides: "In-editor help text aligned with the modular architecture"
      contains:
        - "lua/core/"
        - "lua/plugins/"
        - "lua/custom/plugins/"
    - path: "init.lua"
      provides: "Top-of-file onboarding banner aligned with the thin-entrypoint architecture"
    - path: "lua/custom/plugins/init.lua"
      provides: "Fork-local plugin namespace guidance pointing to the refreshed README story"
    - path: "doc/tags"
      provides: "Regenerated help tag index for the updated help doc"
  key_links:
    - from: "doc/kickstart.txt"
      to: "doc/tags"
      via: ":helptags doc"
      pattern: "kickstart\\.nvim"
    - from: "init.lua"
      to: "lua/story/bootstrap.lua"
      via: "entrypoint handoff text"
      pattern: "story\\.bootstrap"
    - from: "lua/custom/plugins/init.lua"
      to: "README.md"
      via: "customization pointer"
      pattern: "README"
---

<objective>
Refresh the remaining user-facing documentation surfaces so help docs and inline comments teach the same modular architecture as the README, while keeping runtime compatibility names stable.

Purpose: Close the stale-guidance gap outside `README.md` and keep Neovim help tags valid after doc updates.
Output: Updated help and inline guidance in `doc/kickstart.txt`, `init.lua`, `lua/custom/plugins/init.lua`, plus regenerated `doc/tags`.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-documentation-refresh/05-CONTEXT.md
@.planning/phases/05-documentation-refresh/05-RESEARCH.md
@.planning/phases/05-documentation-refresh/05-VALIDATION.md
@.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
@doc/kickstart.txt
@doc/tags
@init.lua
@lua/custom/plugins/init.lua
@lua/kickstart/health.lua
@lua/story/bootstrap.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the shipped help doc around the modular architecture</name>
  <read_first>
    .planning/phases/05-documentation-refresh/05-CONTEXT.md
    .planning/phases/05-documentation-refresh/05-RESEARCH.md
    .planning/phases/05-documentation-refresh/05-VALIDATION.md
    doc/kickstart.txt
    doc/tags
    lua/story/bootstrap.lua
    lua/custom/plugins/init.lua
    .planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
  </read_first>
  <files>doc/kickstart.txt</files>
  <acceptance_criteria>
    `rg -n "lua/core/|lua/plugins/|lua/custom/plugins/" doc/kickstart.txt` returns matches.
    `rg -n "single-file|single file" doc/kickstart.txt` returns no matches.
    The help text keeps the existing `*kickstart.nvim*` topic stable while describing it as the help entrypoint for the modular fork, not a monolithic starter, per D-01, D-02, and deferred rebrand scope.
  </acceptance_criteria>
  <action>
    Rewrite `doc/kickstart.txt` so it aligns with the refreshed modular architecture story without broad runtime rebranding. Keep the existing help topic/tag identity stable, but update the prose to describe the repo as a modular personal or forkable Neovim configuration per D-01 and D-02. Add concise guidance that points readers to `lua/core/`, `lua/plugins/`, and `lua/custom/plugins/` per D-03, and describe `lua/kickstart/plugins/` as compatibility or example-only per D-04. Do not rename live health-provider or module identifiers that Phase 04 intentionally preserved.
  </action>
  <verify>
    <automated>sh -c "rg -n 'lua/core/|lua/plugins/|lua/custom/plugins/' doc/kickstart.txt && ! rg -n 'single-file|single file' doc/kickstart.txt"</automated>
  </verify>
  <done>
    The shipped help doc teaches the modular layout, keeps compatibility naming stable, and no longer implies the single-file model is the intended architecture.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh inline guidance in `init.lua` and `lua/custom/plugins/init.lua`</name>
  <read_first>
    .planning/phases/05-documentation-refresh/05-CONTEXT.md
    .planning/phases/05-documentation-refresh/05-RESEARCH.md
    init.lua
    lua/custom/plugins/init.lua
    lua/story/bootstrap.lua
    README.md
  </read_first>
  <files>init.lua, lua/custom/plugins/init.lua</files>
  <acceptance_criteria>
    `rg -n "story.bootstrap|lua/core/|lua/plugins/|lua/custom/plugins/" init.lua lua/custom/plugins/init.lua` returns matches.
    `rg -n "kickstart init.lua|rest of the kickstart init.lua|See the kickstart.nvim README" init.lua lua/custom/plugins/init.lua` returns no matches.
    The `init.lua` banner presents the file as a thin entrypoint and points readers toward the modular ownership boundaries per D-02 and D-03.
  </acceptance_criteria>
  <action>
    Replace the large onboarding banner in `init.lua` with concise guidance that matches the current runtime architecture: `init.lua` sets early globals and hands off to `require('story.bootstrap').setup()`, while the main customization surfaces live under `lua/core/`, `lua/plugins/`, and `lua/custom/plugins/` per D-02 and D-03. Update `lua/custom/plugins/init.lua` so its comment points readers to the refreshed modular README guidance instead of the old Kickstart framing. Keep the edits practical per D-05 and avoid broad branding or runtime identifier changes that were deferred.
  </action>
  <verify>
    <automated>sh -c "rg -n 'story.bootstrap|lua/core/|lua/plugins/|lua/custom/plugins/' init.lua lua/custom/plugins/init.lua && ! rg -n 'kickstart init.lua|rest of the kickstart init.lua|See the kickstart.nvim README' init.lua lua/custom/plugins/init.lua"</automated>
  </verify>
  <done>
    The inline guidance surfaces no longer teach the monolithic Kickstart model and now point users toward the correct modular ownership boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate help tags and run the phase validation suite</name>
  <read_first>
    .planning/phases/05-documentation-refresh/05-VALIDATION.md
    doc/kickstart.txt
    doc/tags
    init.lua
    lua/custom/plugins/init.lua
    README.md
  </read_first>
  <files>doc/tags</files>
  <acceptance_criteria>
    `nvim --headless -u NONE -i NONE '+helptags doc' +qall` exits 0.
    `sh -c "rg -n \"lua/core/|lua/plugins/|lua/custom/plugins/|story.bootstrap\" README.md doc/kickstart.txt init.lua lua/custom/plugins/init.lua && ! rg -n \"single-file|single file\" README.md doc/kickstart.txt init.lua lua/custom/plugins/init.lua && nvim --headless '+checkhealth kickstart' +qall"` exits 0.
    `git diff -- doc/tags` shows the regenerated tag index if `doc/kickstart.txt` changed.
  </acceptance_criteria>
  <action>
    If `doc/kickstart.txt` changed, regenerate `doc/tags` with `nvim --headless -u NONE -i NONE '+helptags doc' +qall` to satisfy the help-doc integrity requirement from research and the validation contract. Then run the full Phase 05 validation suite from `05-VALIDATION.md`: structural grep checks for `story.bootstrap`, `lua/core/`, `lua/plugins/`, and `lua/custom/plugins/`, the negative grep for `single-file|single file`, and `nvim --headless '+checkhealth kickstart' +qall` to confirm the docs still point at a live troubleshooting surface.
  </action>
  <verify>
    <automated>sh -c "nvim --headless -u NONE -i NONE '+helptags doc' +qall && rg -n 'lua/core/|lua/plugins/|lua/custom/plugins/|story.bootstrap' README.md doc/kickstart.txt init.lua lua/custom/plugins/init.lua && ! rg -n 'single-file|single file' README.md doc/kickstart.txt init.lua lua/custom/plugins/init.lua && nvim --headless '+checkhealth kickstart' +qall"</automated>
  </verify>
  <done>
    Help tags are regenerated, the phase validation suite passes, and no user-facing guidance surface still recommends the single-file model.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader -> help and inline guidance | Users rely on help docs and inline comments to find supported customization surfaces |
| authored help doc -> generated tags | Changes in `doc/kickstart.txt` must propagate into `doc/tags` for Neovim help lookups to remain valid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | `doc/kickstart.txt`, `init.lua`, `lua/custom/plugins/init.lua` | mitigate | Rewrite stale guidance so active customization paths are `lua/core/`, `lua/plugins/`, and `lua/custom/plugins/`, while legacy `lua/kickstart/plugins/` stays compatibility/example-only per D-03/D-04. |
| T-05-04 | D | `doc/tags` | mitigate | Regenerate tags with `nvim --headless -u NONE -i NONE '+helptags doc' +qall` whenever help text changes and verify the full validation suite passes. |
</threat_model>

<verification>
Run the full validation command from `05-VALIDATION.md` at plan completion:

```bash
sh -c "rg -n \"lua/core/|lua/plugins/|lua/custom/plugins/|story.bootstrap\" README.md doc/kickstart.txt init.lua lua/custom/plugins/init.lua && ! rg -n \"single-file|single file\" README.md doc/kickstart.txt init.lua lua/custom/plugins/init.lua && nvim --headless -u NONE -i NONE '+helptags doc' +qall && nvim --headless '+checkhealth kickstart' +qall"
```
</verification>

<success_criteria>
All remaining user-facing guidance surfaces outside the README are aligned with the modular architecture, `doc/tags` matches the updated help doc, and compatibility-facing Kickstart names remain stable unless wording changes are required for accuracy.
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-refresh/05-documentation-refresh-02-SUMMARY.md`
</output>
