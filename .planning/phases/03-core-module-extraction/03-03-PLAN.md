---
phase: 03-core-module-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - lua/core/autocmds.lua
  - lua/story/bootstrap.lua
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - "Always-on autocommands now live in a dedicated core module instead of inline bootstrap code."
    - "The yank highlight autocommand still registers in the `kickstart-highlight-yank` group during headless startup."
    - "Bootstrap clearly separates core editor behavior from runtime-only concerns such as scheduled clipboard sync, repo runtimepath prepend, and lazy.nvim setup."
  artifacts:
    - path: "lua/core/autocmds.lua"
      provides: "Always-on non-plugin autocommands"
      contains: "vim.api.nvim_create_autocmd('TextYankPost'"
    - path: "lua/story/bootstrap.lua"
      provides: "Final ordered core/runtime boundary"
      contains: "require('core.autocmds').setup()"
  key_links:
    - from: "lua/story/bootstrap.lua"
      to: "lua/core/autocmds.lua"
      via: "require('core.autocmds').setup()"
      pattern: "require\\('core\\.autocmds'\\)\\.setup\\(\\)"
---

<objective>
Extract the always-on yank highlight autocommand into a dedicated core module and leave bootstrap as the explicit runtime orchestrator.

Purpose: Satisfy `CORE-03` while making the final startup path readable: core modules first, runtime-only helpers second, plugin setup last.
Output: `lua/core/autocmds.lua` and a final `lua/story/bootstrap.lua` that shows the phase-complete module boundaries.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-core-module-extraction/03-CONTEXT.md
@.planning/phases/03-core-module-extraction/03-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-core-module-extraction/03-02-PLAN.md
@lua/story/bootstrap.lua
@lua/plugins/lsp.lua
@lua/plugins/coding.lua

<interfaces>
Core module call order before this plan:
```lua
require('core.options').setup()
require('core.diagnostics').setup()
require('core.keymaps').setup()
vim.schedule(function() vim.o.clipboard = 'unnamedplus' end)
```

Plugin-local autocommands that must remain outside this plan:
```lua
vim.api.nvim_create_autocmd('LspAttach', { ... })
vim.api.nvim_create_autocmd('FileType', { ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated always-on autocmd module</name>
  <files>lua/core/autocmds.lua</files>
  <read_first>
lua/story/bootstrap.lua
lua/plugins/lsp.lua
lua/plugins/coding.lua
.planning/phases/03-core-module-extraction/03-RESEARCH.md
.planning/codebase/CONVENTIONS.md
  </read_first>
  <acceptance_criteria>
`rg -n "function M.setup\\(\\)" lua/core/autocmds.lua` returns one match.
`rg -n "vim\\.api\\.nvim_create_autocmd\\('TextYankPost'|kickstart-highlight-yank|vim\\.hl\\.on_yank" lua/core/autocmds.lua` returns the yank highlight autocommand, group, and callback.
`rg -n "LspAttach|FileType" lua/core/autocmds.lua` returns no matches.
  </acceptance_criteria>
  <action>Create `lua/core/autocmds.lua` with `local M = {}`, `function M.setup() ... end`, and `return M`. Move the existing always-on yank highlight autocmd from bootstrap into this module exactly as `vim.api.nvim_create_autocmd('TextYankPost', { desc = 'Highlight when yanking (copying) text', group = vim.api.nvim_create_augroup('kickstart-highlight-yank', { clear = true }), callback = function() vim.hl.on_yank() end, })`. Preserve the short explanatory comment about yank highlighting. Do not move plugin-local autocommands such as `LspAttach`, `CursorHold`, `LspDetach`, or `FileType` from `lua/plugins/lsp.lua` or `lua/plugins/coding.lua`. Do not move the scheduled clipboard assignment or runtimepath/lazy bootstrap logic; those stay in bootstrap per D-04 and D-05.</action>
  <verify>
    <automated>rg -n "vim\\.api\\.nvim_create_autocmd\\('TextYankPost'|kickstart-highlight-yank|vim\\.hl\\.on_yank" lua/core/autocmds.lua</automated>
  </verify>
  <done>`lua/core/autocmds.lua` owns the yank highlight autocommand and excludes plugin-local autocommands.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize bootstrap as the explicit core-versus-runtime orchestrator</name>
  <files>lua/story/bootstrap.lua</files>
  <read_first>
lua/story/bootstrap.lua
lua/core/options.lua
lua/core/diagnostics.lua
lua/core/keymaps.lua
lua/core/autocmds.lua
lua/story/bootstrap/lazy.lua
  </read_first>
  <acceptance_criteria>
`rg -n "require\\('core\\.options'\\)\\.setup\\(\\)|require\\('core\\.diagnostics'\\)\\.setup\\(\\)|require\\('core\\.keymaps'\\)\\.setup\\(\\)|require\\('core\\.autocmds'\\)\\.setup\\(\\)" lua/story/bootstrap.lua` returns all four calls.
`rg -n "vim\\.api\\.nvim_create_autocmd\\('TextYankPost'|kickstart-highlight-yank" lua/story/bootstrap.lua` returns no matches.
`rg -n "vim\\.schedule\\(function\\(\\) vim\\.o\\.clipboard = 'unnamedplus' end\\)|vim\\.opt\\.rtp:prepend\\(config_root\\)|require\\('story\\.bootstrap\\.lazy'\\)\\.setup\\(\\)|require\\('lazy'\\)\\.setup\\(" lua/story/bootstrap.lua` returns all runtime-only lines.
`nvim --headless -u ./init.lua \"+lua vim.wait(50); local ac=vim.api.nvim_get_autocmds({ group = 'kickstart-highlight-yank' }); assert(#ac > 0); assert(vim.o.clipboard == 'unnamedplus'); assert(vim.tbl_contains(vim.opt.rtp:get(), vim.fn.getcwd()))\" \"+checkhealth kickstart\" +qa` exits 0.
  </acceptance_criteria>
  <action>Update `lua/story/bootstrap.lua` so `M.setup()` calls `require('core.autocmds').setup()` after `require('core.keymaps').setup()`. Remove the inline `TextYankPost` autocmd block completely. Keep the runtime-oriented lines in bootstrap exactly: `vim.schedule(function() vim.o.clipboard = 'unnamedplus' end)`, `local source = debug.getinfo(1, 'S').source:sub(2)`, `local config_root = vim.fs.dirname(vim.fs.dirname(vim.fs.dirname(source)))`, `vim.opt.rtp:prepend(config_root)`, `require('story.bootstrap.lazy').setup()`, and the existing `require('lazy').setup(...)` block. Add or keep short section comments so the file reads in this order: core module setup, runtime clipboard/bootstrap helpers, plugin manager setup. Do not introduce a dynamic module loader or a loop over module names; explicit `require('core.*').setup()` calls are required to mitigate startup-order regressions.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+lua vim.wait(50); local ac=vim.api.nvim_get_autocmds({ group = 'kickstart-highlight-yank' }); assert(#ac > 0); assert(vim.o.clipboard == 'unnamedplus'); assert(vim.tbl_contains(vim.opt.rtp:get(), vim.fn.getcwd()))" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>`lua/story/bootstrap.lua` explicitly calls all four `lua/core/` modules, no longer defines the yank highlight autocmd inline, and still passes the headless startup plus `checkhealth` gate with repo runtimepath intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lua/story/bootstrap.lua` → `lua/core/autocmds.lua` | Bootstrap must delegate the always-on autocmd without losing registration timing. |
| `lua/core/autocmds.lua` → Neovim autocmd API | Wrong augroup names or event registration would silently drop user-visible behavior. |
| `lua/story/bootstrap.lua` → repo runtimepath / lazy bootstrap | Runtime helpers remain startup-critical and must stay explicit after the structural extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | autocmd registration timing | mitigate | Call `require('core.autocmds').setup()` from bootstrap before runtimepath/lazy setup changes and prove the `kickstart-highlight-yank` group exists in headless startup. |
| T-03-08 | E | module loading | mitigate | Keep four static `require('core.*').setup()` calls in bootstrap and forbid dynamic require loops or directory scanning. |
| T-03-09 | R | silent runtime-helper drift | mitigate | Keep clipboard scheduling, repo runtimepath prepend, lazy bootstrap helper, and `require('lazy').setup(...)` inline in bootstrap and verify them with direct headless assertions plus `checkhealth kickstart`. |
</threat_model>

<verification>
Run the task-level headless startup assertion and `+checkhealth kickstart`, then confirm the final file boundary with structural `rg` checks against `lua/story/bootstrap.lua` and `lua/core/autocmds.lua`.
</verification>

<success_criteria>
`CORE-03` is satisfied when `lua/core/autocmds.lua` owns the always-on yank highlight autocmd, bootstrap visibly separates core modules from runtime-only helpers, and the final headless startup plus `checkhealth kickstart` command succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-module-extraction/03-core-module-extraction-03-SUMMARY.md`
</output>
