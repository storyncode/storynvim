---
phase: 04-compatibility-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lua/story/bootstrap.lua
  - lua/kickstart/plugins/gitsigns.lua
autonomous: true
requirements:
  - PLUG-03
  - PAR-03
must_haves:
  truths:
    - "User-owned plugin specs under `lua/custom/plugins/` load through the modular bootstrap without becoming a second shipped source of truth."
    - "Legacy `kickstart.plugins.*` modules stay available as explicit example or overlay opt-ins instead of an imported active namespace."
    - "Kickstart-facing health and module names remain intact while optional-module compatibility is reconciled."
  artifacts:
    - path: "lua/story/bootstrap.lua"
      provides: "Canonical lazy.nvim spec wiring for shipped plugins, user extensions, and legacy/example guidance"
      contains: "{ import = 'custom.plugins' }"
    - path: "lua/kickstart/plugins/gitsigns.lua"
      provides: "Legacy overlay spec that composes with the shipped gitsigns plugin"
      contains: "optional = true"
  key_links:
    - from: "lua/story/bootstrap.lua"
      to: "lua/custom/plugins/init.lua"
      via: "lazy spec import"
      pattern: "\\{ import = 'custom\\.plugins' \\}"
    - from: "lua/story/bootstrap.lua"
      to: "lua/kickstart/plugins/gitsigns.lua"
      via: "commented explicit opt-in guidance"
      pattern: "require 'kickstart\\.plugins\\.gitsigns'"
---

<objective>
Make the optional/example entrypoints compatible with the modular bootstrap without introducing a second shipped plugin namespace.

Purpose: Satisfy `PLUG-03` and the health-facing part of `PAR-03` by making `custom.plugins` the stable active extension path per D-01 and D-03 while keeping `kickstart.plugins.*` as explicit legacy/example opt-ins per D-02, D-04, and D-05.
Output: Updated bootstrap import guidance and a legacy gitsigns overlay module that composes cleanly with the shipped `lua/plugins` source.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
@.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
@.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/phases/02-plugin-import-layout/02-plugin-import-layout-03-SUMMARY.md
@.planning/phases/03-core-module-extraction/03-core-module-extraction-03-SUMMARY.md
@init.lua
@lua/story/bootstrap.lua
@lua/custom/plugins/init.lua
@lua/kickstart/plugins/gitsigns.lua
@lua/kickstart/health.lua

<interfaces>
From `lua/story/bootstrap.lua`:
```lua
require('lazy').setup({
  spec = { { import = 'plugins' } },

  -- require 'kickstart.plugins.debug'
  -- require 'kickstart.plugins.lint'
  -- require 'kickstart.plugins.gitsigns'
  -- { import = 'custom.plugins' },
}, { ui = { icons = vim.g.have_nerd_font and {} or { ... } } })
```

From `lua/custom/plugins/init.lua`:
```lua
---@module 'lazy'
---@type LazySpec
return {}
```

From `lua/kickstart/plugins/gitsigns.lua`:
```lua
return {
  'lewis6991/gitsigns.nvim',
  opts = {
    on_attach = function(bufnr)
      ...
    end,
  },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `custom.plugins` the active stable extension path without changing the shipped plugin source</name>
  <files>lua/story/bootstrap.lua</files>
  <read_first>
lua/story/bootstrap.lua
lua/custom/plugins/init.lua
.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
.planning/phases/02-plugin-import-layout/02-CONTEXT.md
  </read_first>
  <acceptance_criteria>
`rg -n "\\{ import = 'plugins' \\}" lua/story/bootstrap.lua` returns one match.
`rg -n "\\{ import = 'custom\\.plugins' \\}" lua/story/bootstrap.lua` returns one active spec entry match.
`rg -n "\\{ import = 'kickstart\\.plugins' \\}" lua/story/bootstrap.lua` returns no matches.
`rg -n "require 'kickstart\\.plugins\\.(debug|indent_line|lint|autopairs|neo-tree|gitsigns)'" lua/story/bootstrap.lua` returns the legacy/example opt-in comment lines.
`rg -n "stable user extension path|legacy/example" lua/story/bootstrap.lua` returns updated compatibility wording, not the old "Uncomment the following line" instruction for `custom.plugins`.
  </acceptance_criteria>
  <action>Update `lua/story/bootstrap.lua` so `require('lazy').setup({ spec = { ... } })` keeps `{ import = 'plugins' }` as the only active shipped plugin source per D-02 and adds `{ import = 'custom.plugins' }` as the active user extension path per D-01 and D-03. Rewrite only the nearby guidance comments needed to reflect the true runtime shape: `lua/kickstart/plugins/*.lua` remains an explicit legacy/example opt-in surface, while `lua/custom/plugins/*.lua` is now the stable user extension path. Do not add `{ import = 'kickstart.plugins' }`, do not move files out of `lua/custom/plugins/`, and do not rename any Kickstart-facing module or health strings per D-04 and D-05.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+doautocmd VimEnter" "+lua vim.wait(500); assert(type(require('kickstart.plugins.debug'))=='table'); assert(type(require('kickstart.plugins.lint'))=='table'); assert(type(require('kickstart.plugins.gitsigns'))=='table'); assert(type(require('kickstart.plugins.autopairs'))=='table'); assert(type(require('kickstart.plugins.indent_line'))=='table'); assert(type(require('kickstart.plugins.neo-tree'))=='table'); assert(type(require('custom.plugins'))=='table')" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>`lua/story/bootstrap.lua` actively imports `custom.plugins`, continues to import only `plugins` for shipped specs, and still exposes the `kickstart.plugins.*` examples as commented opt-ins instead of an imported namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Make the legacy gitsigns module an explicit overlay instead of a competing primary spec</name>
  <files>lua/kickstart/plugins/gitsigns.lua</files>
  <read_first>
lua/kickstart/plugins/gitsigns.lua
lua/plugins/editor.lua
lua/story/bootstrap.lua
.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
`rg -n "'lewis6991/gitsigns.nvim'" lua/kickstart/plugins/gitsigns.lua` returns one match.
`rg -n "optional = true" lua/kickstart/plugins/gitsigns.lua` returns one match.
`rg -n "kickstart\\.plugins\\.gitsigns|git \\[s\\]tage hunk|\\[T\\]oggle git show \\[b\\]lame line" lua/kickstart/plugins/gitsigns.lua` returns the existing legacy module name and keymap descriptions unchanged.
`rg -n "lewis6991/gitsigns.nvim" lua/plugins/editor.lua` still returns the shipped base spec.
  </acceptance_criteria>
  <action>Update `lua/kickstart/plugins/gitsigns.lua` so the returned LazySpec keeps the plugin name `lewis6991/gitsigns.nvim` but adds `optional = true` near the top-level spec table. This makes the legacy module behave as an explicit overlay on the shipped gitsigns plugin loaded from `lua/plugins/editor.lua`, matching the Phase 4 overlay model from D-02 and D-03. Preserve the file path, module name, comment header, keymaps, and descriptions exactly; do not rename the module or move it under `lua/plugins/`.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+doautocmd VimEnter" "+lua vim.wait(500); assert(type(require('kickstart.plugins.gitsigns'))=='table'); assert(type(require('custom.plugins'))=='table'); local cfg=require('lazy.core.config'); assert(cfg.plugins['gitsigns.nvim'] or cfg.plugins['lewis6991/gitsigns.nvim'])" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>The legacy `kickstart.plugins.gitsigns` module remains at the same path and name, but now declares itself as an optional overlay that composes with the shipped gitsigns spec instead of acting like a separate primary source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lua/story/bootstrap.lua` → `lazy.nvim` spec import graph | Bootstrap decides which plugin namespaces are active and must not create a second shipped source by accident. |
| `lua/story/bootstrap.lua` → `lua/custom/plugins/*.lua` | User-owned extension files become active at startup and must stay isolated from shipped plugin ownership. |
| `lua/kickstart/plugins/gitsigns.lua` → shipped gitsigns spec | The legacy module must merge as an overlay rather than fork plugin ownership or break keymaps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | bootstrap spec imports | mitigate | Keep `{ import = 'plugins' }` as the only shipped source, add only `{ import = 'custom.plugins' }`, and forbid any `{ import = 'kickstart.plugins' }` entry. |
| T-04-02 | E | user extension namespace | mitigate | Preserve `custom.plugins` at the existing path and validate direct `require('custom.plugins')` under the repo entrypoint. |
| T-04-03 | R | legacy overlay composition | mitigate | Mark `lua/kickstart/plugins/gitsigns.lua` as `optional = true`, preserve its module name, and verify the shipped gitsigns plugin still exists in `lazy.core.config`. |
</threat_model>

<verification>
Use the validation contract from `04-VALIDATION.md` for task execution: run `nvim --headless -u ./init.lua "+checkhealth kickstart" +qa` after each task and rerun the direct legacy-module require suite after Task 2.
</verification>

<success_criteria>
`PLUG-03` and the compatibility half of `PAR-03` are satisfied when `custom.plugins` is active as the stable extension path, `kickstart.plugins.*` remains legacy/example-only, `kickstart.plugins.gitsigns` composes as an overlay, and `checkhealth kickstart` still succeeds without renamed surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/04-compatibility-and-cleanup/04-compatibility-and-cleanup-01-SUMMARY.md`
</output>
