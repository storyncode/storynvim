---
phase: 04-compatibility-and-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - lua/story/bootstrap.lua
autonomous: true
requirements:
  - PAR-01
  - PAR-03
must_haves:
  truths:
    - "Bootstrap no longer repeats dead or duplicated runtimepath/startup logic inline when the same behavior can be expressed once and re-used."
    - "The repo-entrypoint runtimepath invariant still holds before and after `lazy.setup(...)`."
    - "Cleanup leaves legacy/example guidance and Kickstart-facing health surfaces intact while removing only code proven duplicate."
  artifacts:
    - path: "lua/story/bootstrap.lua"
      provides: "Finalized ordered startup file with explicit compatibility-safe runtimepath handling"
      contains: "ensure_config_root_on_rtp"
  key_links:
    - from: "lua/story/bootstrap.lua"
      to: "lazy.setup(...)"
      via: "helper call before and after plugin-manager setup"
      pattern: "ensure_config_root_on_rtp\\(config_root\\)"
---

<objective>
Perform the conservative cleanup pass in bootstrap after compatibility is proven, removing only duplicated startup logic and outdated boundary comments.

Purpose: Satisfy the cleanup half of `PAR-01` and `PAR-03` per D-08 and D-09 by reducing obvious duplication without touching proven runtime behavior.
Output: A cleaned-up `lua/story/bootstrap.lua` that preserves repo-entrypoint startup behavior and the legacy/example guidance surface.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
@.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
@.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
@.planning/phases/04-compatibility-and-cleanup/04-02-PLAN.md
@.planning/phases/03-core-module-extraction/03-core-module-extraction-03-SUMMARY.md
@lua/story/bootstrap.lua
@lua/story/bootstrap/lazy.lua
@lua/kickstart/health.lua

<interfaces>
Current duplicate runtimepath behavior in `lua/story/bootstrap.lua`:
```lua
vim.opt.rtp:prepend(config_root)
...
require('lazy').setup({ ... })
...
vim.opt.rtp:prepend(config_root)
```

Required invariant from Phase 03 summaries:
```lua
assert(vim.tbl_contains(vim.opt.rtp:get(), vim.fn.getcwd()))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace duplicate runtimepath mutations with one explicit helper</name>
  <files>lua/story/bootstrap.lua</files>
  <read_first>
lua/story/bootstrap.lua
lua/story/bootstrap/lazy.lua
.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
.planning/phases/03-core-module-extraction/03-core-module-extraction-03-SUMMARY.md
  </read_first>
  <acceptance_criteria>
`rg -n "local function ensure_config_root_on_rtp\\(config_root\\)" lua/story/bootstrap.lua` returns one match.
`rg -n "ensure_config_root_on_rtp\\(config_root\\)" lua/story/bootstrap.lua` returns exactly two call sites plus the helper definition.
`rg -n "^\\s*vim\\.opt\\.rtp:prepend\\(config_root\\)$" lua/story/bootstrap.lua` returns no direct inline calls outside the helper.
`rg -n "debug\\.getinfo|vim\\.uv\\.fs_realpath|require\\('story\\.bootstrap\\.lazy'\\)\\.setup\\(\\)|require\\('lazy'\\)\\.setup\\(" lua/story/bootstrap.lua` still returns the existing startup-critical lines.
  </acceptance_criteria>
  <action>Refactor `lua/story/bootstrap.lua` to introduce a local helper named `ensure_config_root_on_rtp(config_root)` that performs `vim.opt.rtp:prepend(config_root)`. Replace the two existing inline `vim.opt.rtp:prepend(config_root)` statements with helper calls: one before `require('story.bootstrap.lazy').setup()` and one immediately after `require('lazy').setup(...)`. Do not alter the real-path resolution, clipboard scheduling, `lazy.nvim` bootstrap, or the plugin spec table. This is the only code-path cleanup in the plan because D-08 allows removing duplication only when headless verification proves the behavior is still necessary and intact.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+doautocmd VimEnter" "+lua vim.wait(500); assert(vim.o.clipboard == 'unnamedplus'); assert(vim.tbl_contains(vim.opt.rtp:get(), vim.fn.getcwd())); require('story.compat').assert_phase04_full()" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>`lua/story/bootstrap.lua` expresses runtimepath preservation through a single helper and still preserves repo-entrypoint parity before and after plugin-manager setup.</done>
</task>

<task type="auto">
  <name>Task 2: Remove only obsolete bootstrap wording that conflicts with the finalized module boundaries</name>
  <files>lua/story/bootstrap.lua</files>
  <read_first>
lua/story/bootstrap.lua
.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
lua/kickstart/health.lua
  </read_first>
  <acceptance_criteria>
`rg -n "Core editor behavior|Runtime helpers|Plugin manager bootstrap" lua/story/bootstrap.lua` returns the three section headings.
`rg -n "Uncomment the following line and add your plugins" lua/story/bootstrap.lua` returns no matches.
`rg -n "require 'kickstart\\.plugins\\.(debug|lint|gitsigns)'" lua/story/bootstrap.lua` still returns the legacy/example comment lines.
`rg -n "kickstart\\.nvim|checkhealth kickstart" lua/kickstart/health.lua` still returns the existing Kickstart-facing health surface unchanged.
  </acceptance_criteria>
  <action>Trim only the bootstrap comments that became false after Plan 04-01 and Task 1. Keep the section headings `Core editor behavior`, `Runtime helpers`, and `Plugin manager bootstrap` explicit so the file ends in the Phase 04 boundary shape. Delete outdated wording that tells users to uncomment `custom.plugins` manually, but keep the commented `require 'kickstart.plugins.*'` examples and other Kickstart-facing help text intact per D-04, D-05, D-08, and D-09. Do not remove tutorial comments elsewhere in `init.lua`, do not touch `lua/kickstart/health.lua`, and do not rename any module paths.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+doautocmd VimEnter" "+lua vim.wait(500); assert(vim.o.clipboard == 'unnamedplus'); assert(vim.fn.maparg('<leader>sh', 'n', false, true).lhs == '<Space>sh'); require('story.compat').assert_phase04_full()" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>Only the obsolete bootstrap wording is removed; the startup sections are explicit, legacy/example comments remain, and the full parity suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lua/story/bootstrap.lua` → runtimepath handling | Cleanup must preserve the repo-entrypoint path invariant before and after plugin-manager startup. |
| `lua/story/bootstrap.lua` → user guidance comments | Comment cleanup can accidentally erase extension-path or legacy opt-in guidance if not kept conservative. |
| cleanup pass → validated parity state | This plan must not widen scope beyond the behaviors already approved in Plan 04-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T | runtimepath preservation | mitigate | Replace duplicated `vim.opt.rtp:prepend(config_root)` lines with one helper but keep both call sites and verify repo-entrypoint runtimepath plus full parity afterwards. |
| T-04-08 | R | cleanup overreach | mitigate | Restrict cleanup to `lua/story/bootstrap.lua`, remove only outdated `custom.plugins` wording, and explicitly preserve `kickstart.plugins.*` comment guidance plus `lua/kickstart/health.lua` names. |
| T-04-09 | D | post-cleanup regression | mitigate | Re-run `require('story.compat').assert_phase04_full()` with `checkhealth kickstart` after each cleanup task. |
</threat_model>

<verification>
Use the `04-03-01` validation command shape plus `require('story.compat').assert_phase04_full()` after each task so cleanup stays subordinate to the already-approved parity suite.
</verification>

<success_criteria>
Phase 04 cleanup is complete when `lua/story/bootstrap.lua` contains no duplicated inline runtimepath mutations, no false `custom.plugins` instructions, still preserves the legacy/example opt-in comments, and the full automated parity suite plus `checkhealth kickstart` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-compatibility-and-cleanup/04-compatibility-and-cleanup-03-SUMMARY.md`
</output>
