---
phase: 04-compatibility-and-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lua/story/compat.lua
autonomous: false
requirements:
  - PAR-01
  - PAR-02
  - PAR-03
must_haves:
  truths:
    - "The repo can prove core startup parity and plugin workflow parity through one repeatable headless assertion entrypoint."
    - "Telescope, LSP attach behavior, completion, formatting, Treesitter, health checks, and optional-module requires are all explicitly sampled before cleanup proceeds."
    - "A human has verified the remaining interactive editor behaviors that headless checks cannot fully prove."
  artifacts:
    - path: "lua/story/compat.lua"
      provides: "Reusable Phase 04 parity assertions for headless Neovim"
      contains: "function M.assert_phase04_full()"
  key_links:
    - from: "lua/story/compat.lua"
      to: "lua/plugins/telescope.lua"
      via: "keymap and plugin registry assertions"
      pattern: "telescope\\.nvim"
    - from: "lua/story/compat.lua"
      to: "lua/plugins/lsp.lua"
      via: "LspAttach and conform assertions"
      pattern: "nvim%-lspconfig"
    - from: "lua/story/compat.lua"
      to: "lua/kickstart/plugins/*.lua"
      via: "direct require assertions for legacy/example modules"
      pattern: "kickstart\\.plugins"
---

<objective>
Codify the Phase 04 parity contract into one headless assertion module, then gate cleanup on a short human verification pass for the interactive workflows.

Purpose: Satisfy D-06 and D-07 by making parity checks explicit and repeatable before any cleanup lands, covering `PAR-01`, `PAR-02`, and `PAR-03`.
Output: `lua/story/compat.lua` plus a blocking manual parity sign-off task for Telescope, completion, formatting, Treesitter, and LSP attach behavior.
</objective>

<execution_context>
@/Users/danieldavies/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danieldavies/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-compatibility-and-cleanup/04-CONTEXT.md
@.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
@.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@.planning/phases/04-compatibility-and-cleanup/04-01-PLAN.md
@lua/story/bootstrap.lua
@lua/plugins/telescope.lua
@lua/plugins/lsp.lua
@lua/plugins/coding.lua
@lua/plugins/editor.lua
@lua/kickstart/health.lua

<interfaces>
Validation contract from `04-VALIDATION.md`:
```text
nvim --headless -u ./init.lua "+doautocmd VimEnter" "+doautocmd BufWritePre" "+lua vim.wait(500); ..." "+checkhealth kickstart" +qa
```

Interactive surfaces that must remain intact:
```lua
vim.keymap.set('n', '<leader>sh', builtin.help_tags, ...)
vim.api.nvim_create_autocmd('LspAttach', { ... })
require('conform').format { async = true, lsp_format = 'fallback' }
vim.api.nvim_create_autocmd('FileType', { ... treesitter_try_attach ... })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a reusable Phase 04 parity assertion module</name>
  <files>lua/story/compat.lua</files>
  <read_first>
lua/story/bootstrap.lua
lua/plugins/telescope.lua
lua/plugins/lsp.lua
lua/plugins/coding.lua
lua/plugins/editor.lua
lua/kickstart/health.lua
.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
.planning/phases/04-compatibility-and-cleanup/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
`rg -n "local M = \\{|function M\\.assert_phase04_full\\(\\)|return M" lua/story/compat.lua` returns all three lines.
`rg -n "telescope\\.nvim|nvim%-lspconfig|blink\\.cmp|conform\\.nvim|nvim%-treesitter|gitsigns\\.nvim" lua/story/compat.lua` returns the shipped plugin registry assertions.
`rg -n "kickstart\\.plugins\\.debug|kickstart\\.plugins\\.lint|kickstart\\.plugins\\.gitsigns|kickstart\\.plugins\\.autopairs|kickstart\\.plugins\\.indent_line|kickstart\\.plugins\\.neo%-tree|custom\\.plugins" lua/story/compat.lua` returns the legacy and custom module assertions.
`rg -n "<leader>sh|<leader><leader>|kickstart%-highlight%-yank|LspAttach|clipboard == 'unnamedplus'" lua/story/compat.lua` returns the runtime parity assertions.
  </acceptance_criteria>
  <action>Create `lua/story/compat.lua` with `local M = {}` and a single exported function `M.assert_phase04_full()`. Inside that function, encode the headless parity contract from `04-VALIDATION.md`: assert the `lazy.core.config` registry contains `telescope.nvim`, `nvim-lspconfig`, `blink.cmp`, `conform.nvim`, `nvim-treesitter`, and `gitsigns.nvim`; assert `type(require('kickstart.plugins.debug')) == 'table'`, `type(require('kickstart.plugins.lint')) == 'table'`, `type(require('kickstart.plugins.gitsigns')) == 'table'`, `type(require('kickstart.plugins.autopairs')) == 'table'`, `type(require('kickstart.plugins.indent_line')) == 'table'`, `type(require('kickstart.plugins.neo-tree')) == 'table'`, and `type(require('custom.plugins')) == 'table'`; assert `vim.fn.maparg('<leader>sh', 'n', false, true).lhs == '<Space>sh'`; assert `vim.fn.maparg('<leader><leader>', 'n', false, true).lhs == '<Space><Space>'`; assert `#vim.api.nvim_get_autocmds({ group = 'kickstart-highlight-yank' }) > 0`; assert `#vim.api.nvim_get_autocmds({ event = 'LspAttach' }) > 0`; assert `vim.o.clipboard == 'unnamedplus'`; and assert `pcall(require, 'conform')`. Keep the module read-only and assertion-only: no side effects, no bootstrap rewiring, and no plugin configuration changes.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+doautocmd VimEnter" "+doautocmd BufWritePre" "+lua vim.wait(500); require('story.compat').assert_phase04_full()" "+checkhealth kickstart" +qa</automated>
  </verify>
  <done>`lua/story/compat.lua` exists as the single automated Phase 04 parity entrypoint and passes the full headless suite from the validation contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm interactive workflow parity before cleanup</name>
  <files>lua/story/compat.lua</files>
  <read_first>
lua/story/compat.lua
lua/plugins/telescope.lua
lua/plugins/lsp.lua
lua/plugins/coding.lua
.planning/phases/04-compatibility-and-cleanup/04-VALIDATION.md
  </read_first>
  <acceptance_criteria>
The user confirms all five checks: Telescope help search opens with `<leader>sh`; a Lua buffer shows Treesitter highlighting; completion suggestions appear while typing; `<leader>f` formats a modifiable Lua buffer; and LSP attach mappings such as `grn` or `grr` exist after the language server starts.
No new interactive regressions are reported for health checks or optional-module loading.
  </acceptance_criteria>
  <action>Run the automated parity suite first, then pause for the manual parity pass required by D-06 and D-07. Keep the checkpoint focused on the editor behaviors headless Neovim cannot fully prove. Use these exact verification steps:
1. Launch `nvim -u ./init.lua`.
2. Press `<leader>sh` and confirm the Telescope help picker opens.
3. Open a Lua file in the repo, verify Treesitter highlighting is active, and type enough text to confirm the completion popup appears.
4. Save or run `<leader>f` in that Lua buffer and confirm formatting still works.
5. Wait for the Lua language server to attach, then confirm one LSP mapping such as `grn` rename or `grr` references is available.
If any step fails, resume with the failure details instead of approval.</action>
  <verify>
    <automated>nvim --headless -u ./init.lua "+doautocmd VimEnter" "+doautocmd BufWritePre" "+lua vim.wait(500); require('story.compat').assert_phase04_full()" "+checkhealth kickstart" +qa</automated>
  </verify>
  <what-built>Automated parity coverage for startup, health, lazy-loaded plugin surfaces, and legacy/custom module loading via `lua/story/compat.lua`.</what-built>
  <how-to-verify>
1. Start `nvim -u ./init.lua`.
2. Trigger `<leader>sh` and confirm Telescope help search opens.
3. Open a Lua buffer and confirm Treesitter highlighting plus completion suggestions appear while typing.
4. Run `<leader>f` or save the buffer and confirm formatting completes.
5. After the Lua LSP attaches, confirm an LSP mapping like `grn` or `grr` works or is present.
  </how-to-verify>
  <resume-signal>Type `approved` to continue to cleanup, or describe the regression you observed.</resume-signal>
  <done>Cleanup may proceed only after the automated parity suite passes and the user explicitly approves the interactive workflow checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lua/story/compat.lua` → runtime bootstrap and plugin graph | The parity module reads live runtime state and must faithfully reflect the required behaviors without mutating startup. |
| headless verification → lazy-loaded event surfaces | Event-driven plugins can hide regressions unless the correct events are triggered before assertions. |
| automated parity → human interactive verification | Headless checks cannot fully prove interactive picker, completion, formatting UX, or visible syntax highlighting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | D | lazy event coverage | mitigate | Trigger `VimEnter` and `BufWritePre` before calling `require('story.compat').assert_phase04_full()` so event-loaded behavior is sampled. |
| T-04-05 | R | false sense of parity | mitigate | Keep `lua/story/compat.lua` assertion-only, match the exact validation contract from `04-VALIDATION.md`, and require a blocking human verification step for interactive workflows. |
| T-04-06 | T | health/module compatibility drift | mitigate | Include `checkhealth kickstart` plus direct `require(...)` assertions for all legacy/example modules and `custom.plugins` in the automated suite. |
</threat_model>

<verification>
The automated gate is the full-suite command from `04-VALIDATION.md`, rewritten to call `require('story.compat').assert_phase04_full()`. Manual UAT is mandatory before execution can continue to Plan 04-03.
</verification>

<success_criteria>
`PAR-01`, `PAR-02`, and `PAR-03` are satisfied for Phase 04 execution once the parity module passes the full headless suite and the user approves the interactive workflow checklist without reporting regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-compatibility-and-cleanup/04-compatibility-and-cleanup-02-SUMMARY.md`
</output>
